import { UnirNombresPipe } from './unir-nombres.pipe';

describe('UnirNombresPipe', () => {
  it('create an instance', () => {
    const pipe = new UnirNombresPipe();
    expect(pipe).toBeTruthy();
  });
});
